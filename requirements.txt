# Core dependencies for Anaglyph stereo microscopy application
# Install: pip install -r requirements.txt
# For camera VID/PID discovery (recommended on Windows): pip install -r requirements.txt cv2-enumerate-cameras
opencv-python>=4.8.0
numpy>=1.24.0
PyQt6>=6.5.0
